import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles(() => ({
  root: {
    height: 200,
    width: 350,
    padding: 20,
    borderRadius: 8,
    border: '1px solid #dbdbdb',
  },
}));
